{
  "name": "portfolio-production",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "python scripts/build.py",
    "dev": "wrangler dev",
    "check:worker": "wrangler deploy --dry-run --outdir .artifacts/worker",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.132.0",
    "@playwright/test": "1.58.2"
  },
  "dependencies": {
    "gsap": "3.12.5",
    "leaflet": "1.9.4",
    "nunjucks": "3.2.4"
  }
}
